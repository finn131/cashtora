{
  "name": "cashtora",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd client/server && node src/index.js & cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client/server && node src/index.js",
    "test": "cd client/server && npm test",
    "lint": "echo 'No linter configured. Run: cd client/server && npm test'"
  }
}
